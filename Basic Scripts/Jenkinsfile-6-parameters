pipeline {
    agent any
    parameters {
        string defaultValue: 'abc', description: 'Enter the value', name: 'FirstName'
    }
    stages {
        stage('Start') {
            steps {
                echo "Your name is: $params.FirstName"
            }
        }
    }
}